const Loading = () => {
    return (
        <div className="w-[800px] h-[600px] absolute bg-slate-500 bg-opacity-50 z-50 flex flex-col items-center justify-center">
        </div>
    );
};

export default Loading;